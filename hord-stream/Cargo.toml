[package]
name = "hord-stream"
description = "HORD stream layer: message envelope, credit flow control, byte-stream over RDMA send/recv"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
hord-core = { path = "../hord-core" }
